{
  "name": "almundo-frontend-exercise-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "author": "Eduardo",
  "dependencies": {
    "body-parser": "^1.19.0",
    "config": "^3.2.2",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-query-int": "^3.0.0",
    "morgan": "^1.9.1",
    "mysql": "^2.17.1",
    "nvm": "0.0.4",
    "sequelize": "^5.15.0",
    "sequelize-cli": "^3.2.0",
    "serve-favicon": "^2.5.0",
    "sqlite3": "^4.0.9",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "cross-env": "^5.2.0",
    "jshint": "^2.10.2",
    "mocha": "^6.2.0",
    "nodemon": "^1.19.1",
    "nyc": "^14.1.1"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=development ./node_modules/sequelize-cli/bin/sequelize --seeders db:seed:all npm run server",
    "start-prod": "cross-env NODE_ENV=production npm run server",
    "server": "node server.js",
    "test": "cross-env-shell NODE_ENV=test npm run mocha",
    "mocha": "mocha --recursive --exit",
    "jshint": "jshint app/",
    "coverage": "nyc --reporter=html --reporter=text mocha --recursive --exit"
  }
}
